# 本地测试用 docker-compose 配置
# 用于在本地模拟云托管环境（包含 MySQL 和 PostgreSQL）
version: '3.8'

services:
  # 主应用
  app:
    build: .
    ports:
      - "8123:8123"
    environment:
      # MySQL 配置
      MYSQL_URL: jdbc:mysql://mysql:3306/super_ai_agent?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      MYSQL_USERNAME: root
      MYSQL_PASSWORD: root123
      # PostgreSQL 配置
      POSTGRESQL_URL: jdbc:postgresql://postgres:5432/super_ai_agent
      POSTGRESQL_USERNAME: postgres
      POSTGRESQL_PASSWORD: postgres123
      # AI API Key
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      # 其他配置
      SEARCH_API_KEY: ${SEARCH_API_KEY}
      TAVILY_SEARCH_API_KEY: ${TAVILY_SEARCH_API_KEY}
      SPRING_PROFILES_ACTIVE: cloud
    depends_on:
      mysql:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - super-ai-network

  # 前端应用
  web:
    build: ./super-ai-agent-web
    ports:
      - "5123:5123"
    volumes:
      - ./super-ai-agent-web/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - super-ai-network

  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: super-ai-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: super_ai_agent
    ports:
      - "3306:3306"
    volumes:
      - /你的宿主机目录/super-ai-agent-data/mysql/data/:/var/lib/mysql
      - ./src/main/resources/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    networks:
      - super-ai-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot123"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # PostgreSQL 向量数据库
  postgres:
    image: pgvector/pgvector:pg16
    container_name: super-ai-postgres
    environment:
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: super_ai_agent
    ports:
      - "5432:5432"
    volumes:
      - /你的宿主机目录/super-ai-agent-data/postgresql/data/:/var/lib/postgresql/data/
      - ./src/main/resources/pgvector.sql:/docker-entrypoint-initdb.d/pgvector.sql
    networks:
      - super-ai-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  super-ai-network:
    driver: bridge


